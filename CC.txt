1.sudo apt-get update -y

sudo apt-get install -y curl wget unzip tar git jq


2.curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
rm -rf awscliv2.zip aws/



aws --version


3.
KUBECTL_VERSION=$(curl -sL https://dl.k8s.io/release/stable.txt)
curl -LO "https://dl.k8s.io/release${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
chmod +x kubectl
sudo mv kubectl /usr/local/bin/kubectl
kubectl version --client



sudo rm -f /usr/local/bin/kubectl



curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
chmod +x kubectl
sudo mv kubectl /usr/local/bin/
kubectl version --client



4.curl --silent --location "https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_$(uname -s)_amd64.tar.gz" \
| tar xz -C /tmp

sudo mv /tmp/eksctl /usr/local/bin
eksctl version


5.aws configure

AKIA4RC2QWMG5CVKT_G4D a k
ZBs7zY7uxaqwEwKSwNpDMUYMAHJ61kFCwUiEW_uCr s a ke
eu-north-1
json



6.eksctl create cluster \
--name my-eks-pro1 \
--region eu-north-1 \
--nodegroup-name standard-workers \
--node-type t3.medium \
--nodes 1 \
--nodes-min 1 \
--nodes-max 1 \
--managed






//it is a pod file text which is used to create containers
kind: Pod
apiVersion: v1
metadata:
  name: myfirspod
spec:
  containers:
    - name: apachecon
      image: httpd
      ports:
        - containerPort: 80



//To see cluster names
kubectl config get-clusters




apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80




After aws configure only we use these all aws cli commands
aws ec2 describe-key-pairs
//to know key pairs

aws s3 ls
//to know buckets


aws ec2 run-instances \
--image-id ami-042b4708b1d05f512 \
--instance-type t3.micro \
--key-name a \
--count 1 \
--tag-specifications "ResourceType=instance,Tags=[{Key=Name,Value=DevOps-Server}]"    
//To create an ec2 instance using command


aws ec2 describe-instances
//To know instances

aws ec2 terminate-instances --instance-ids i-0123456789abcdef0
//TO terminate instance

aws ec2 stop-instances --instance-ids <INSTANCE_ID>
//to stop instances


aws s3 mb s3://bucket-name
 or
aws s3 mb s3://bindu-devops-2026-001 --region eu-north-1
//to make bucket

aws s3 rb s3://bucket-name
//to remove bucket



